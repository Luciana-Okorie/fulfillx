apiVersion: apps/v1
kind: Deployment
metadata:
  name: fulfillment-service
  namespace: fulfillx
spec:
  replicas: 2
  selector:
    matchLabels: { app: fulfillment-service }
  template:
    metadata:
      labels: { app: fulfillment-service }
    spec:
      containers:
        - name: fulfillment-service
          image: fulfillx/fulfillment-service:latest
          ports: [{ containerPort: 8083 }]
          envFrom:
            - configMapRef: { name: fulfillx-config }
          env:
            - name: DATABASE_URL
              valueFrom: { secretKeyRef: { name: fulfillx-db-credentials, key: DATABASE_URL_FULFILLMENT } }
            - name: KAFKA_CONSUMER_GROUP
              value: "fulfillment-service"
            - name: PORT
              value: "8083"
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "500m", memory: "512Mi" }
          readinessProbe:
            httpGet: { path: /readyz, port: 8083 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: 8083 }
            initialDelaySeconds: 10
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: fulfillment-service
  namespace: fulfillx
spec:
  selector: { app: fulfillment-service }
  ports: [{ port: 8083, targetPort: 8083 }]
